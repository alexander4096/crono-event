Si Cambia el idioma

main*
edit_crono*
edit_alarma*
edit_evento*
edit_config
historico_pausa*
ayuda*

---------------
NO usan cambio de idioma
ver_alarma
ver_evento
acerca

--------------

self.lang
---------------

   if not  self.main_window is None: # si existe clase main_window ejecuta el metodo
                self.main_window.actualizar_submenu() # actualizar submenu de cronometro en clase MainWindow_Cronometro

main
if not self.ventanas[1] is None: # existe ventana Edit_crono
if not self.ventanas[3] is None: # existe ventana ayuda
if not self.ventanas[4] is None: # existe ventana Edit_evento
if not self.ventanas[6] is None: # existe ventana historico_pausa
if not self.ventanas[7] is None: # existe ventana EditarAlarma


-------------------------
 # VENTANA Configuracion (disable)
            if index == 5:
                self.ventanas[index] = EditConfig_class(self) # pasa self
                print("ventana configuracion")

